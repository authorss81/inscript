# v3.9.6.16 — Multi-file breakpoints (.ins tests)
# Run: python inscript.py --test v3.9.6.16/test_multi_file.ins

test "simple breakpoint syntax" {
    assert(true)
}

test "line number arithmetic" {
    assert(42 == 42)
}

test "file:line concept" {
    assert(true)
}

test "source line referencing" {
    assert(true)
}
