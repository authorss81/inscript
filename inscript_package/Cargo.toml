[workspace]
members = [
    "rust_vm_engine",
    "inscript_rust_parser",
]
resolver = "2"

# Legacy flat lexer (kept for backward compat; superseded by inscript_rust_parser)
# [package]
# name = "inscript-lexer"
# version = "3.7.2"
